// const { test, expect } = require("@playwright/test");

// test("Context Menu", async ({ page }) => {
//   await page.goto("/context_menu", { waitUntil: "load" });
//   await expect(page).toHaveURL("/context_menu");
//   await expect(page.locator("h3")).toHaveText("Context Menu");
//   const contextArea = page.getByRole("hotspot");
//   await contextArea.click({ button: "right" });
//   console.log("Test Passed");
// });
